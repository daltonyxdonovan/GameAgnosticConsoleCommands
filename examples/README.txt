If I keep the example scripts named '.cs', it borks compilation of the project as a whole, so I have
simply renamed the extension's end to '.cs.example'. If you remove the '.example', it will open just
like any other script! :)